# Gemini key for live generation (tests need no key or network)
GEMINI_API_KEY=your-key-here
# Set to 1 to enable the local telemetry/debug endpoints; leave unset in any public deploy
DEBUG_ENDPOINTS=0
