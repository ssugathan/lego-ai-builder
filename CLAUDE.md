@AGENTS.md

## Claude-specific notes

- Slash commands live in `.claude/commands/` (spec-agent, status, place-files).
- When running as a cloud session (claude.ai/code), the Cloud sync and Project state rules in AGENTS.md apply unchanged: fetch first, reconcile PROGRESS.md, never end with unpushed work.
